- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.config/kitty: kitty
    ~/.config/sheldon: sheldon
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.zsh:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
